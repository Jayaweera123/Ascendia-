import React from "react";

const PageTitle = ({ title }) => {
  return (
    <h1 className="mb-4 text-3xl font-bold text-gray-900">{title}</h1> // Increased font size and added margin bottom
  );
};

export default PageTitle;
